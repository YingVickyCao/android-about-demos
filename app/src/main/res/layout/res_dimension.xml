<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/topic"
        style="@style/topic"
        android:text="@string/page_dimension"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/topic">

        <Button
            android:id="@+id/px2dp"
            style="@style/btn_small"
            android:text="@string/px_dp" />

        <Button
            android:id="@+id/dp2px"
            style="@style/btn_small"
            android:text="@string/dp_px" />

        <Button
            android:id="@+id/test_dimen"
            style="@style/btn_small"
            android:text="getDimension vs getDimensionPixelSize vs getDimensionPixelOffset" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>