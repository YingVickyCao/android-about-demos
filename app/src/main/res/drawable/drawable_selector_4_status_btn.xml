<?xml version="1.0" encoding="UTF-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed   -->
    <item android:drawable="@android:color/holo_red_light" android:state_pressed="true" />
    <!-- Android 中没有focus 状态。写了也没有效果   -->
    <!--    <item android:drawable="@android:color/holo_green_light" android:state_focused="true" />-->
    <!-- Android 中没有hover 状态。写了也没有效果   -->
    <!--    <item android:drawable="@android:color/holo_blue_bright" android:state_hovered="true" />-->
    <!-- Default   -->
    <item android:drawable="@android:color/holo_orange_dark" android:state_enabled="true" />
    <!-- Disable   -->
    <item android:drawable="@android:color/darker_gray" android:state_enabled="false" />
</selector>