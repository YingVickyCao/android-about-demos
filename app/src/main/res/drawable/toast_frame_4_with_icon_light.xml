<?xml version= "1.0" encoding ="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!--    <solid android:color="@color/light_bg_1" />-->
    <!-- When light_bg_1, toast 跟 body 区分 不明显，改为light_bg_2 -->
    <solid android:color="@color/light_bg_2" />
    <padding
        android:bottom="20dp"
        android:left="20dp"
        android:right="20dp"
        android:top="20dp" />
    <corners android:radius="2dp" />
</shape>